# TODO Card Body's first line (to \n </p> or <br>) is Card's Name
# TODO register custom SQLite text_for_html(string)→string and
#      name_for_html(string)→string functions
#     (check with Notebox to see if text is case-folded)
# TODO statically link in both build.sh and
#	buildwin.sh (the latter in Deb VM)

cardindex.go
static.go

cardindex_test.go

sql/prepare.sql
sql/create.sql

README.md

go.mod

build.sh
buildwin.sh
st.sh

# TODO on loading and existing .cix execute:
#      BEGIN;
#      UPDATE Config SET Value = Value + 1 WHERE Key = 'N';
#      COMMIT;
# TODO on saving a .cix execute:
#      BEGIN;
#      UPDATE Config SET Value = DATETIME('NOW') WHERE Key = 'Updated';
#      COMMIT;
# and if N > 11 execute:
#      BEGIN;
#      UPDATE Config SET Value = 0 WHERE Key = 'N';
#      COMMIT;
#      INSERT INTO Card_Words(Card_Words) VALUES ('optimize');
#      VACUUM;

# Three MDI window types:
#
# Query        (X)
# /Query\/Cards\
# ... details of query|list of matching cards if any ...
# 0 cards match
#
# Name               (X)
# Body
# ...
#
# Group           (X)
# gid | Name
# ... | ...

# vim: syn=yaml
