# TODO CardNames view: trim to first [.!?\n] then trim trailing WS
cardindex.go
static.go

cardindex_test.go

sql/prepare.sql
sql/create.sql

README.md

go.mod

build.sh
buildwin.sh
st.sh

# TODO on loading and existing .cix execute:
#      BEGIN;
#      UPDATE Config SET Value = Value + 1 WHERE Key = 'N';
#      COMMIT;
# TODO on saving a .cix execute:
#      BEGIN;
#      UPDATE Config SET Value = DATETIME('NOW') WHERE Key = 'Updated';
#      COMMIT;
# and if N > 11 execute:
#      BEGIN;
#      UPDATE Config SET Value = 0 WHERE Key = 'N';
#      COMMIT;
#      INSERT INTO Card_Words(Card_Words) VALUES ('optimize');
#      VACUUM;

# Three MDI window types:
#
# Query        (X)
# ... use a vertical splitter, Query at the top List of cards below ...
# ... details of query|list of matching cards if any ...
# 0 cards match
#
# Name (of Card)  (X)
# Body
# ...
# [Boxes          v] # boxes this card belongs to
# [Linked Cards   v] # cards this card is linked to
#
# Box           (X)
# Name
# ...

# NOTE Markup is CommonMark

# vim: syn=yaml
